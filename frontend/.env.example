# Local development can run without this file; it falls back to the JSON store.
# For Vercel persistence, set one hosted Postgres URL in the Vercel dashboard.
POSTGRES_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"

# Set to false only for a local Postgres server without SSL.
POSTGRES_SSL="true"
